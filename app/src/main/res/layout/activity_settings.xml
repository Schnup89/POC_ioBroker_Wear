<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activity_settings"
    tools:deviceIds="wear">

    <LinearLayout
        android:id="@+id/settings_linear"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="25dp"
        android:paddingTop="25dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="ioBroker Host o. IP"
            android:textColor="@android:color/white"
            android:textSize="12sp"
            android:inputType="textNoSuggestions" />
        <EditText
            android:id="@+id/inp_settings_host"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:layout_marginBottom="12dp"
            android:maxLines="1"
            android:singleLine="true"
            android:text="error" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="Port"
            android:textColor="@android:color/white"
            android:textSize="12sp"
            android:inputType="textNoSuggestions" />
        <EditText
            android:id="@+id/inp_settings_port"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:layout_marginBottom="12dp"
            android:maxLines="1"
            android:singleLine="true"
            android:text="error"
            android:inputType="number"/>

        <Button
            android:id="@+id/inp_settings_save"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="onClick_settings_save"
            android:text="Speichern"
            android:textSize="12sp" />


    </LinearLayout>

</ScrollView>